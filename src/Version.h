/**
 * Application version string.
 * Default: "dev" for local builds.
 * Overwritten by CI (build-and-release workflow) with the release tag (e.g. v1.0.0).
 */
#ifndef VERSION_H
#define VERSION_H
#define APP_VERSION "dev"
#endif
